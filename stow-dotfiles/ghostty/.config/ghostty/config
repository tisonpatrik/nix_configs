theme = catppuccin-mocha
shell-integration = zsh
command = "/run/system-manager/sw/bin/zsh -i"

# Additional configuration for better integration
font-family = "JetBrains Mono"
font-size = 12

# Window configuration
window-theme = system
window-padding-x = 0
window-padding-y = 0